# Active rc9 completion goal

The user resumed the mission and requested a completion goal on 2026-09-07.
The release criterion remains verified replacement coverage. Rafter's approved
private-name retirement is recorded in [DECISIONS.md](DECISIONS.md).

## Completion order

1. Close the assertion inventory across the three frozen repositories and
   independent workspaces. Inspect source-reading checks inside ordinary tests,
   helper branches, registry instances and CI/script entrypoints. Record missing
   requirements before deciding the engine feature set is sufficient.
2. Finish one complete kafka-driver policy bundle and qualification demonstration.
   Close its remaining assertion IDs, preserve all source/input preconditions,
   and run old/new enforcement, full source analysis, coverage and evidence checks.
   Its proposed CI lane must execute zrail enforcement.
3. Complete Kafkars and Rafter translations and the bounded capabilities their
   assertions require. Keep runtime scenarios, independent verification and
   build/release execution. Govern reference workspaces through explicit separate
   contracts and trusted invocations. Prepare exact downstream cutover maps.
4. Resolve compatibility and protected review, version the final tree to rc9,
   and qualify that exact revision through the full canonical gate, parity suites,
   self-hosting, archives and evidence verification. Produce final release notes
   and return `rc9-ready` only when the ledger and full-repository evidence agree.

## Keeping the work focused

- Reuse stock capabilities before adding an engine rule. A newly discovered
  source check may need only a policy and qualification fixtures.
- Work in small, coherent Conventional Commits. Keep source changes, evidence
  identities and verification status distinguishable.
- Run focused diagnostics and compatibility tests for each change. Batch the
  expensive full qualification at committed milestones, with fresh evidence
  directories and pinned source/policy identities. Repeat only when needed to
  establish determinism or qualify changed inputs.
- Audit while independent trusted qualification runs are executing. Keep
  repository analysis bounded, read-only and non-executing.
- Keep unresolved assertions visible. A growing reviewed inventory is discovery
  progress, not a percentage estimate or a regression to conceal.

The reviewed root contract/lock require explicit human authority before grants
can be accepted. No downstream deletion, publication or release tag is authorized.
The current status and immediate continuation are in [CHECKPOINT.md](CHECKPOINT.md).
