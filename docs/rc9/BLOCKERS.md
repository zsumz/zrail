# Open reviewed assertion blockers

Release verdict: **blocked**. This list names each reviewed but unverified
assertion instance. It does not close the additional discovery blockers
`RC9-INVENTORY-KD`, `RC9-INVENTORY-KF`, and `RC9-INVENTORY-RF`.

All unreviewed candidate IDs are enumerated in `census.json.gz`. Non-Rust
files and opaque or invalid syntax remain explicit review work.
`RF-PRIVATE-NAME-PATTERNS` is closed by the user's approved retirement in
[RC9-DECISION-RF-PRIVATE-NAMES](DECISIONS.md).

| Assertion ID | Cause |
| --- | --- |
| `KD-CI-ENFORCEMENT` | The mission explicitly requires replacing this prohibition with an enforced zrail lane. The new lane and its required policy/receipt evidence are not yet qualified. |
| `KD-DEP-DETECTOR-EXACT-NAME` | Import the exact detector fixture and execute its line-name extractor beside native line policies. |
| `KD-DEP-DETECTOR-PRESENT-NAME` | Import the exact detector fixture and execute its line-name extractor beside native line policies. |
| `KD-FACADE-LIVE` | Predicate fixtures pass; full downstream selection and complete declarative policy qualification remain open. |
| `KD-METADATA-PARENT` | Record the path-precondition proof and bind the exact translated license paths before closing this helper assertion. |
| `KD-PROVENANCE-LOCK-PACKAGE-ARRAY` | Translate the exact authored provenance predicate and qualify frozen positive/negative inputs; no complete protocol policy bundle has been verified. |
| `KD-PROVENANCE-LOCK-PACKAGE-NAMES` | Account for the implicit all-entry name-index precondition. Existing Cargo parsing rejects non-string names more strictly; qualify this distinction and preserve complete lock scope. |
| `KD-PROVENANCE-bornera-VERSION-PREFIX` | Preserve the exact-version input condition in canonical native package identity policy and qualify the representation change; a ranged or malformed expected version cannot become authority. |
| `KD-PROVENANCE-bornera-core-VERSION-PREFIX` | Preserve the exact-version input condition in canonical native package identity policy and qualify the representation change; a ranged or malformed expected version cannot become authority. |
| `KD-PROVENANCE-bornera-rustls-VERSION-PREFIX` | Preserve the exact-version input condition in canonical native package identity policy and qualify the representation change; a ranged or malformed expected version cannot become authority. |
| `KD-PROVENANCE-kafka-wire-VERSION-PREFIX` | Preserve the exact-version input condition in canonical native package identity policy and qualify the representation change; a ranged or malformed expected version cannot become authority. |
| `KD-PROVENANCE-kafka-wire-core-VERSION-PREFIX` | Preserve the exact-version input condition in canonical native package identity policy and qualify the representation change; a ranged or malformed expected version cannot become authority. |
| `KD-QUAL-015` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-016` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-017` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-025` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-026` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-027` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-044` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-045` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-046` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-047` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-048` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-049` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-050` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-051` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-052` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-01` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-02` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-03` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-04` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-05` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-06` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-07` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-067-08` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-069` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-081` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-084` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-087` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-089` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-090` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-095` | A bounded native predicate must preserve raw first-occurrence byte order; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-096` | A bounded native predicate must preserve raw first-occurrence byte order; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-097` | A bounded native predicate must preserve literal within raw first-marker interval; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-105` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-106` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-107` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-108` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-109` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-119` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-120` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-121` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-122` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-123` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-124` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-125` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-126` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-127` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-128` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-129` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-130` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-131` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-132` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-133` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-139-01` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-139-02` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-139-03` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-141` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-142` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-143` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-144` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-145` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-146` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-149` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-150` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-151` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-160` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-161` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-162` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-163` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-164` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-165` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-166` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-167` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-168` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-176` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-177` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-178` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-179` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-180` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-181` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-182` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-193` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-197` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-199-01` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-199-02` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-199-03` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-200-01` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-200-02` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-200-03` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-202` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-203` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-204` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-205` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-206` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-207` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-208` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-209` | Translate and qualify the exact frozen literal assertion; matching text does not establish workflow or command execution. |
| `KD-QUAL-228-01` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-228-02` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-228-03` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-228-04` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-228-05` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-228-06` | A bounded native predicate must preserve trimmed line prefix value allowlist; whole-file substring matching cannot replace this assertion. |
| `KD-QUAL-READ-02` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-03` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-04` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-05` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-06` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-07` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-08` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-09` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-10` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-11` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-12` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-13` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-14` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-15` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-16` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-17` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-18` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-19` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-20` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-21` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-22` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-23` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-QUAL-READ-24` | Translate and qualify the expanded frozen input-read precondition alongside the qualification policy; an absent marker alone does not require a readable file. |
| `KD-RETIRED-BACKEND-VARIANT` | Bounded Rust enum-variant predicate and cfg/omission/duplicate fixtures remain open. |
| `KD-RETIRED-CONSTRUCTION-LegacyBackend` | Raw absence predicate exists; the pinned source selection and differential evidence remain to qualify. |
| `KD-RETIRED-CONSTRUCTION-new_legacy` | Raw absence predicate exists; the pinned source selection and differential evidence remain to qualify. |
| `KD-RETIRED-MODULE-broker_set` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-plaintext` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-poller` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-resource` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-tcp` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-timer` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-MODULE-tls` | Required Rust module-declaration absence predicate and adversarial source fixtures remain open. |
| `KD-RETIRED-PARSE-BACKEND` | Bind this existing source parse requirement to the explicit source inventory and prove malformed/absent input failure. |
| `KD-RETIRED-PARSE-MODULES` | Bind this existing source parse requirement to the explicit source inventory and prove malformed/absent input failure. |
| `KD-RETIRED-TREE-broker_set` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-plaintext` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-poller` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-resource` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-tcp` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-timer` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-RETIRED-TREE-tls` | A bounded physical prohibition exists; preserve the exact descendant scope and record stronger explicit read/symlink failure behavior in differential evidence. |
| `KD-TRANSPORT-ASSOCIATED` | Exact inventory predicates, authored alias restrictions, compilation-world rules, and positive/negative full selection qualification remain open. |
| `KD-TRANSPORT-IMPLS` | Exact inventory predicates, authored alias restrictions, compilation-world rules, and positive/negative full selection qualification remain open. |
| `KD-TRANSPORT-METHODS` | Exact inventory predicates, authored alias restrictions, compilation-world rules, and positive/negative full selection qualification remain open. |
| `KD-TRANSPORT-OWNERS` | Exact inventory predicates, authored alias restrictions, compilation-world rules, and positive/negative full selection qualification remain open. |
| `KD-TRANSPORT-RENAMES` | Exact inventory predicates, authored alias restrictions, compilation-world rules, and positive/negative full selection qualification remain open. |
| `KF-BUDGET-ALLOW-PATH` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-ALLOW-PRESENCE` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-ALLOW-UNIQUE` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-BASELINE-PATH` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-BASELINE-PRESENCE` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-BASELINE-UNIQUE` | Predicate implementation exists; exact frozen positive/negative mapping remains to qualify. |
| `KF-BUDGET-DECOY-FIXTURE` | Required detector input must be imported and bound; this assertion is not classified as system behavioral evidence. |
| `KF-BUDGET-NESTED-TESTS` | Future nested path classification passes; the exact nested-decoy-manifest counterexample still needs import and complete Cargo/source qualification. |
| `KF-FACADE-LIVE` | Predicate fixtures pass; full downstream selection and complete declarative policy qualification remain open. |
| `KF-FETCH-ORDER-ACTIVATION` | Bounded code-order predicate is absent; its stronger code-only semantics need explicit difference evidence. An owner allowlist is insufficient. |
| `KF-FETCH-ORDER-CONTROL` | Bounded code-order predicate is absent; its stronger code-only semantics need explicit difference evidence. An owner allowlist is insufficient. |
| `KF-FETCH-ORDER-PREPARE` | Bounded code-order predicate is absent; its stronger code-only semantics need explicit difference evidence. An owner allowlist is insufficient. |
| `KF-FETCH-PRESENCE-01` | Required literal/code predicate translation and omission fixtures remain open. |
| `KF-FETCH-PRESENCE-02` | Required literal/code predicate translation and omission fixtures remain open. |
| `KF-FETCH-PRESENCE-03` | Required literal/code predicate translation and omission fixtures remain open. |
| `KF-SCAN-MINIMUM` | Repository-file assertions and exact source-selection fixtures are required; passing the frozen scan does not supply a replacement guard. |
| `KF-SCAN-ROOTS` | Native repository.roots already checks directory presence; the frozen positive/negative source-selection qualification still needs intended-diagnostic linkage. |
| `RF-FACADE-LIVE` | Rafter permits data declarations; wiring modes would change policy. Exact raw-line or reviewed structural normalization remains open. |
| `RF-PROCESS-ALLOWLIST` | Exact contextual inventory and non-aliasing predicates are not implemented or differentially verified. |
| `RF-PROCESS-CRATE-ALIASES` | Exact contextual inventory and non-aliasing predicates are not implemented or differentially verified. |
| `RF-PROCESS-EXACT` | Exact contextual inventory and non-aliasing predicates are not implemented or differentially verified. |
| `RF-PROCESS-MACROS` | Exact contextual inventory and non-aliasing predicates are not implemented or differentially verified. |
| `RF-PROCESS-SPELLING` | Exact contextual inventory and non-aliasing predicates are not implemented or differentially verified. |
| `RF-TEST-FACADE-LIVE` | Rafter permits data declarations; wiring modes would change policy. Exact raw-line or reviewed structural normalization remains open. |
| `RF-WIRE-ALIAS-ENTRY` | The same final assertion combines expression-shape and raw-text checks; this independent raw-text branch also needs replacement. |
| `RF-WIRE-ALIAS-MEMBERSHIP` | The same final assertion combines expression-shape and raw-text checks; this independent raw-text branch also needs replacement. |
| `RF-WIRE-ALIAS-MSG` | The same final assertion combines expression-shape and raw-text checks; this independent raw-text branch also needs replacement. |
| `RF-WIRE-REGISTRY-LogEntryTag` | Required source/text predicates are not translated. |
| `RF-WIRE-REGISTRY-MembershipTag` | Required source/text predicates are not translated. |
| `RF-WIRE-REGISTRY-MessageTag` | Required source/text predicates are not translated. |
| `RF-WIRE-RESERVATION` | Required documentation literal predicate is not translated. |
| `RF-WIRE-U8-SHAPES` | Call-argument shapes and exact exception parity are not implemented; ownership does not subsume this requirement. |
